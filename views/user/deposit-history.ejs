<%- include('../partials/header') %>

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="gold-text fw-bold">📋 تاریخچه واریزها</h3>
        <a href="/user/deposit" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> واریز جدید
        </a>
    </div>

    <div class="card bg-dark border-0 shadow">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-dark table-striped mb-0">
                    <thead>
                        <tr class="text-warning">
                            <th>#</th>
                            <th>مبلغ (USDT)</th>
                            <th>TxID</th>
                            <th>وضعیت</th>
                            <th>توضیحات</th>
                            <th>تاریخ</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (deposits.length > 0) { %>
                            <% deposits.forEach((tx, index) => { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    <td class="fw-bold text-success"><%= tx.amount.toFixed(2) %></td>
                                    <td class="small text-muted">
                                        <%= tx.txHash ? tx.txHash.slice(0, 10) + '...' : '---' %>
                                    </td>
                                    <td>
                                        <% if (tx.status === 'approved') { %>
                                            <span class="badge bg-success">تأیید شده ✅</span>
                                        <% } else if (tx.status === 'pending') { %>
                                            <span class="badge bg-warning text-dark">در انتظار ⏳</span>
                                        <% } else { %>
                                            <span class="badge bg-danger">رد شده ❌</span>
                                        <% } %>
                                    </td>
                                    <td class="small text-muted"><%= tx.description || '---' %></td>
                                    <td class="small"><%= new Date(tx.date).toLocaleString('fa-IR') %></td>
                                </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="6" class="text-center text-muted py-4">
                                    <i class="bi bi-inbox display-4 d-block mb-3"></i>
                                    هیچ واریزی یافت نشد
                                    <br>
                                    <a href="/user/deposit" class="btn btn-success btn-sm mt-3">واریز تتر</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>
